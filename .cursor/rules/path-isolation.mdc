---
description: 禁止跨包 Read/Shell/对照兄弟产出（路径隔离）
alwaysApply: true
---

# 本包路径隔离（禁止跨包抄袭）

本工作区 = **当前包 git 根**（`*-Swift` / `*-OC` / `*-Flutter`）。产包与维护时必须遵守：

## 硬禁止

1. **禁止** Read / Grep / Glob / Search 任何**其它包**路径，包括但不限于：
   - 同级或上级 `output/` 下其它 `*-Swift|OC|Flutter`
   - `Desktop/` 下其它 App 目录
   - `global-brain` 里其它包的 `_sessions`
2. **禁止**用 Shell（`cat` / `sed` / `rg` / `python` 读文件等）去打开上述路径。
3. **禁止**「对照 / 参考 / 抄」兄弟包的 Plan、HTML、Swift、H5、设计文档或截图。
4. 需要范例时：只用**本包内**已有文件、流水线 `prompts/` / `docs/rules/`（若在本会话上下文中已提供），**不要**自行去磁盘翻其它产出包。

## 允许

- 仅读写**本 git 根及其子目录**内的源码、资源、文档与构建产物。
- 本包相对路径与本包绝对路径（仍落在本 git 根内）。

若提示词要求打开兄弟包：直接拒绝，并说明违反路径隔离规则。
